{
  "name": "cluster-manager",
  "version": "1.0.0",
  "description": "Cluster Manager for Self-Healing Distributed Cache",
  "main": "cluster.js",
  "scripts": {
    "start": "node cluster.js",
    "dev": "nodemon cluster.js",
    "lint": "eslint . --ext .js",
    "test": "echo \"No tests in Phase 1\" && exit 0"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.50.0",
    "nodemon": "^3.0.1"
  }
}
